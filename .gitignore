build/
.lock-waf_linux_build
.clangd

# Local companion config — contains Worker URL and secret, never commit
src/pkjs/config.js

# Worker dependencies and local state
worker/node_modules/
worker/bun.lockb
worker/bun.lock
worker/.wrangler
